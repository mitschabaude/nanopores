the following bash command installs the package in your (unix) system (don't try with windows):

>> python setup.py install

on ubuntu you may need

>> sudo python setup.py install

after doing that, you can
    -) import the "nanopores" package in any python file
    -) run "np-test" from anywhere


